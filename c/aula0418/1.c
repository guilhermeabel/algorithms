/*
1. Escreva um programa que leia dois valores inteiros, k e n, e calcule e mostre na tela um valor S dado por
S = k * 1 + (k+1) * 2 + (k+2) * 3 + ... + (k+n) * (n+1)
*/

#include <stdio.h>
#include <stdlib.h>

int main()
{
    int k, n;
    printf("Digite um valor \n");
    scanf("%d", &k);
    printf("Digite um valor \n");
    scanf("%d", &n);
    
    return 0;
}